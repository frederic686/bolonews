{% extends 'base.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}
<style>
    .admin-wrapper {
        max-width: 800px;
        margin: 2rem auto;
        font-family: sans-serif;
    }

    h1, h2 {
        color: #333;
    }

    .form-section {
        background-color: #f9f9f9;
        padding: 1.5rem;
        margin-bottom: 2rem;
        border-radius: 8px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    button {
        margin-top: 1rem;
    }
</style>

<div class="admin-wrapper">
    <h1>Interface d'administration 🛠️</h1>

    <div class="form-section">
        <h2>Créer une catégorie</h2>
        {{ form_start(formCategorie) }}
            {{ form_row(formCategorie.libelle) }}
            <button class="btn btn-success">Ajouter la catégorie</button>
        {{ form_end(formCategorie) }}
    </div>

    <div class="form-section">
        <h2>Créer un article</h2>
        {{ form_start(formArticle) }}
            {{ form_row(formArticle.titre) }}
            {{ form_row(formArticle.chapeau) }}
            {{ form_row(formArticle.contenu) }}
            {{ form_row(formArticle.image) }}
            {{ form_row(formArticle.categorie) }}
            {{ form_row(formArticle.publie) }}
            <button class="btn btn-primary">Ajouter l’article</button>
        {{ form_end(formArticle) }}
    </div>
</div>
{% endblock %}
