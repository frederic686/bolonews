<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Bolonews{% endblock %}</title>

    <link rel="stylesheet" href="css/styles.css">
    {% block stylesheets %}{% endblock %}
</head>
<body>

    {% block header %}
        {% set current_route = app.request.attributes.get('_route') %}

        <header>
            <nav class="navbar">
                <div class="logo">
                    <a href="{{ path('app_accueil') }}">📰 Bolonews</a>
                </div>

                <ul class="nav-links">
                    <li><a href="{{ path('app_accueil') }}">Home</a></li>
                    <li><a href="{{ path('app_article') }}">Articles</a></li>
                </ul>

                <ul class="auth-links">
                    {% if app.user %}
                        <li><span>👤 {{ app.user.pseudo }}</span></li>
                        <li><a href="{{ path('app_logout') }}">Se déconnecter</a></li>
                    {% elseif current_route in ['app_accueil', 'app_article'] %}
                        <li><a href="{{ path('app_login') }}">Se connecter</a></li>
                        <li><a href="{{ path('app_register') }}">M’inscrire</a></li>
                    {% endif %}
                </ul>
            </nav>
        </header>
    {% endblock %}

    <main>
        {% block body %}{% endblock %}
    </main>

    {% block javascripts %}{% endblock %}
</body>
</html>
