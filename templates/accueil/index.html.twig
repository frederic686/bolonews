{% extends 'base.html.twig' %}

{% block title %}Accueil Bolonews{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Derniers articles publiés</h1>

    {% if articles is empty %}
        <p>Aucun article publié pour le moment.</p>
    {% else %}
        {% for article in articles %}
            <div class="article-card">
                <h3>{{ article.titre }}</h3>
                <p><strong>Catégorie :</strong> {{ article.categorie.libelle }}</p>
                <p><em>{{ article.chapeau }}</em></p>
                <div>{{ article.contenu|nl2br }}</div>

                {% if article.image %}
                    <div style="margin: 1em 0;">
                        <img src="{{ asset('uploads/images/' ~ article.image) }}" alt="illustration" style="max-width:100%; height:auto;">
                    </div>
                {% endif %}

                <p>🕒 Publié le {{ article.dateCreation|date('d/m/Y H:i') }}</p>
                <p>👤 Auteur : {{ article.auteur.pseudo ?? 'Inconnu' }}</p>
            </div>
        {% endfor %}
    {% endif %}
</div>
{% endblock %}
